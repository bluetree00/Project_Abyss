# 인수인계 문서 — 2026-06-27 (dev/KBG-D)

> 이 문서는 2026-06-27 세션 작업을 다른 사람/다른 세션이 이어받기 위한 핸드오프입니다.
> 모든 상태는 작성 시점에 `git log`/`git status`/`ls`로 실측해 반영했습니다. 추측은 "확인 필요"로 표시합니다.
> **코드는 이 세션에서 추가 수정/커밋하지 않았습니다. 이 문서(.md) 1개만 생성했습니다.**

---

## 1. 개요

| 항목 | 값 |
|---|---|
| 브랜치 | `dev/KBG-D` |
| HEAD 커밋 | `32b9b899da78bf09e5540ee30c8ff6d84f327164` |
| HEAD 메시지 | `chore: 레거시 死코드/에셋 일괄 정리 + 문서 정정` (2026-06-27 14:27:32 +0900) |
| 업스트림 | `origin/dev/KBG-D` |
| ahead / behind | **ahead 1 / behind 0** → HEAD 1개가 아직 push 안 됨 |
| 컴파일 상태 | 마지막 커밋 시점 에러 0 (확인 필요: 미커밋 효과텍스트 A~D 변경분은 Unity refresh/read_console 재검증 권장) |

**중요**: 현재 working tree에 미커밋 변경이 다수 있음(아래 §5). 특히 **진행 중 작업(효과 텍스트 UX, §3)은 전부 미커밋 상태**입니다.

---

## 2. 이번 세션 완료 작업 (영역별, 커밋 여부 명시)

### 2-1. 상점방 버그 2건 수정 — **커밋됨**
- 무기 SOLD 조기 확정 / 상점 롤 `roomRng` 결정성 관련 수정.
- 상점 NPC UI/판매 전환 작업은 커밋 **`a7fc1ab67`** `feat: 상점 NPC UI·우주 배경·DNF 폰트·챕터 런 구조 등 작업 통합` (2026-06-17)에 포함되어 HEAD의 조상(ancestor)으로 확인됨.
- 신규 파일(모두 tracked, 커밋 완료):
  - `Assets/RelicFairy/Systems/Stage/Shop/ShopNpcInteraction.cs`
  - `Assets/RelicFairy/UI/Popup/ShopUIStyle.cs`
  - `Assets/RelicFairy/UI/Popup/UI_ShopPanel.cs` (+ `UI_ShopPanel.prefab`)
  - `Assets/RelicFairy/UI/Popup/UI_ShopSlotView.cs`
- 레거시 월드 매대 `ShopStallInteraction` 마커 축소도 커밋된 작업에 포함(확인 필요: 정확한 커밋 핸들은 a7fc1ab67 계열).
- 백업: `C:\tmp\shop_backup_20260616\` (존재 확인됨)

### 2-2. @UIRoot 레거시 死 UI 제거 — **커밋됨**
- LoginRoot / ResultRoot / Panel_explore / EditView 제거.
- 백업: `C:\tmp\uiroot_backup_20260616_155016\` (존재 확인됨)

### 2-3. 로그인 레거시 .cs/프리팹 삭제 — **커밋됨**
- UI_Login / UI_Result / GridEditView / UITextInterAction / FindID / FindPw / RegisterAccount + 팝업/고아 프리팹.
- 관련 커밋: `4a88c84d2` `chore: 로그인/계정 레거시 정리 …` (HEAD 조상).
- 실측 확인: `git ls-files`에 위 로그인 레거시 파일 **0건** (정상 제거됨).
- 백업: `C:\tmp\login_legacy_backup_20260617_130308\` (존재 확인됨)

### 2-4. 레거시 1·2순위 死 코드/에셋 일괄 정리 — **커밋 `32b9b899d` (= HEAD)**
- 묘비 5 + 死 스크립트 18 + 어빌리티 SO 섬 + 퀘스트 v1 섬 + 대체쌍 3 등.
- 실측 커밋 통계: **89 files changed, 182 insertions(+), 2574 deletions(-)** (`git show --stat 32b9b899d`).
- 백업: `C:\tmp\legacy_cleanup_20260627_120252\` (존재 확인됨)

### 2-5. 메모리 노트 정정 5건 — (메모리, 리포 외부)
1. 서약 구버전 8종 삭제 완료
2. 세이브 SteamCloud 미착수 · 무결성 HMAC는 불일치 시 거부 아닌 경고만
3. "최적화 PR1~9" 명명 오류 · "방 빌드 분산" 미구현(MapBuilder 전부 동기 for)
4. RoomBuff 폐기 아님·실제 활성
5. R스킬 부분 활성/도달 가능

### 2-6. 출시 체크리스트 생성 — **커밋됨**
- `docs/release-checklist.md` (실측 존재, 19068 bytes). 커밋 `32b9b899d`에서 169줄 추가됨.

---

## 3. 진행 중 작업 = 효과 텍스트 UX 개선 (★ 인계 핵심, 전부 미커밋)

5단계 계획 **A → B → C → D → E** 중 **A/B/C/D 완료, E 미착수**. 모든 변경 **미커밋**.

### 아키텍처 (중요)
- 기존 **동작용** `ItemEffectRegistry`(효과의 실제 게임 로직)와 **분리된 "표시 전용(display-only) 레이어"** 를 신설.
- 신규 코드는 전부 신설 폴더 `Assets/RelicFairy/Shared/Effect/` 에 위치 (git상 untracked 폴더 `?? Assets/RelicFairy/Shared/Effect/`).
- 표시 레이어는 비침습(non-invasive) 글루 방식: 기존 UI는 단일 진입점 `EffectDescriptionFormatter.Describe(...)` 와 위젯만 호출.

### A+B (1단계) — 메타/포매터 + 배선
- 신설 `EffectMetaRegistry.cs` (21,406 bytes): `effectType → { 한글 라벨, 카테고리, 단위, 아이콘키 }` 매핑. 실측 약 **109~110 엔트리**.
- 신설 `EffectDescriptionFormatter.cs` (10,729 bytes): 단일 진입점 `Describe`, 단위 일원화(예: `+4` / `+400%`).
- 배선(미커밋 수정 파일, 실측 grep으로 참조 확인):
  - `ItemInfoPanel.cs` — `EffectRowWidget` 참조 (상세)
  - `UI_ItemAcquisitionPopup.cs` — `EffectRowWidget` 참조 (획득 팝업)
  - `InventoryPageView.cs` — `EffectDescriptionFormatter` 참조 (상세·툴팁)
  - `CombatPanelView.cs` — `EffectDescriptionFormatter` + `EffectIconView` 참조 (HUD 버프)

### C (2단계) — 아이콘 레이어
- 신설 `EffectIconSetSO.cs` (2,018 bytes): 아트 교체 슬롯(ScriptableObject).
- 신설 `EffectIconRegistry.cs` (8,037 bytes): `IconKey → Sprite`, 없으면 플레이스홀더 색 토큰.
- 신설 `EffectIconView.cs` (1,867 bytes): 비침습 글루 컴포넌트.
- **실제 아이콘 아트는 아직 없음 → 전부 플레이스홀더.**
- 교체법: `EffectIconSet.asset` 생성 → 키별 스프라이트 할당 → Addressable 키 `UI/EffectIconSet` 로 등록.

#### IconKey 어휘 목록 (실측: `EffectIconRegistry.cs` 내 문자열 키)
속성: `fire`, `ice`, `lightning`, `poison`, `element` (+ 대문자 토큰 `FIRE`/`ICE`/`ELECTRIC`/`GRASS`)
스탯/전투: `atk`, `def`, `hp`, `crit`, `critdmg`, `atkspeed`, `speed`, `range`, `cooldown`, `dmg`, `allstats`
상태/유틸: `shield`, `heal`, `lifesteal`, `stun`, `freeze`, `roll`, `projectile`, `skill`, `gold`, `luck`, `special`, `utility`, `unknown`
플레이스홀더 관련: `EffectTokenPlaceholder`

### D (3단계) — 공통 위젯
- 신설 `EffectRowWidget.cs` (5,904 bytes, + `EffectRowStyle`): 효과 1줄 공통 위젯.
- 위젯화 적용: `UI_ItemAcquisitionPopup`, `ItemInfoPanel`.
- **보류**: HUD 버프(bg 칩 구조라 위젯 형태 다름), 인벤 상세/툴팁(단일 TMP 리치텍스트, dark 스타일 별도 필요).

### E (미착수 — 다음 작업자가 할 일)
1. **누락 지점 위젯 적용**(출처별 어댑터): 상점 카드 / 룬 / 유물 PassiveSO / 서약 하드코딩 / 스킬 `SkillTooltipTrigger`.
2. **전투 플로팅 어휘 통합**: `GuidelineVisualRunner` 의 `statusId → 한글/속성색` 을 `EffectMetaRegistry`/`ElementDef` 로 단일화.
3. **DNF 폰트 마무리**: `QuestTracker.prefab` / `QuestNotifier.prefab` 의 `NotoSansKR → DNF` 전환.

---

## 4. 알려진 이슈 / 미해결 결정 필요

### 4-1. DNF 폰트 SDF 미커밋 churn
- 파일: `Assets/RelicFairy/Fonts/DNFForgedBlade-Bold SDF.asset` (working tree 수정됨, 미커밋).
- **손상 아님.** 실측: `m_AtlasPopulationMode: 1`(Dynamic), `m_ClearDynamicDataOnBuild: 1`(ON).
- 원인: Dynamic 폰트인데 `ClearDynamicDataOnBuild=1` 이라 빌드/전처리가 글리프 캐시를 비움 → 매번 diff churn 발생.
- 실측 diff: **9 insertions(+), 645 deletions(-)** (글리프 캐시가 비워진 형태).
- 런타임 렌더는 정상(.ttf 정상, HEAD 커밋본에는 글리프 캐시 존재).
- 옵션:
  - **A) `git checkout` 으로 SDF 복원** (HEAD 캐시본 유지) — 안전.
  - **C) 플래그 `ClearDynamicDataOnBuild` OFF** — 단, 의도 확인 필요(빌드 용량/동적 캐시 정책 영향).
- 현재 결정 보류 → **미커밋으로 남겨둠.**

### 4-2. 레거시 3순위 (콘텐츠 결정 선행 필요, 삭제 미실행)
- DeathKnight / Dragon 미사용 보스 세트 (프리팹 약 11 + SO 다수)
- Bamao UI 팩 (약 119 프리팹)
- → 기획 결정 대기. **삭제하지 말 것** (결정 전).

### 4-3. `.claude/agents` stale 참조 2줄 (자동 분류기 차단으로 미정정)
- `.claude/agents/client.md:45` — `PlayerAbility/PlayerAbilitySetSO.cs` (삭제된 SO 참조)
- `.claude/agents/uiux.md:31` — `UILobbyData.cs` (삭제된 데이터 구조체 참조)
- 둘 다 실측 확인됨. 자동 분류기 차단으로 이번 세션에서 정정 못함.

### 4-4. 효과텍스트 보류분
- 상점 카드(레이아웃 여유 0), 룬/유물/서약/스킬은 출처별 어댑터 필요(§3 E단계).

### 4-5. 시각 검증 미완
- CLI에서 이미지 확인 불가. 위젯/아이콘 레이아웃은 **에디터 플레이로 최종 확인 필요**.

---

## 5. 미커밋 변경 목록 (git status 실측)

### 효과텍스트 A~D — 신규 파일 (untracked, `Assets/RelicFairy/Shared/Effect/`)
- `EffectMetaRegistry.cs` (+ .meta)
- `EffectDescriptionFormatter.cs` (+ .meta)
- `EffectIconSetSO.cs` (+ .meta)
- `EffectIconRegistry.cs` (+ .meta)
- `EffectIconView.cs` (+ .meta)
- `EffectRowWidget.cs` (+ .meta)
- (폴더 .meta: `?? Assets/RelicFairy/Shared/Effect.meta`)

### 효과텍스트 A~D — 수정 파일 (modified, tracked)
- `Assets/RelicFairy/UI/HUD/HUDScripts/CombatPanelView.cs`
- `Assets/RelicFairy/UI/Popup/InventoryPageView.cs`
- `Assets/RelicFairy/UI/Popup/UI_ItemAcquisitionPopup.cs`
- `Assets/RelicFairy/UI/PuzzleGrid/Scripts/ItemInfoPanel.cs`

### 기타 미커밋 (효과텍스트와 무관 — 분리 커밋/제외 판단 필요)
- ` M Assets/RelicFairy/Fonts/DNFForgedBlade-Bold SDF.asset` — 폰트 SDF churn (§4-1, 보류)
- ` M .claude/agents/lead.md` — 에이전트 설정
- ` M .claude/scheduled_tasks.lock` — 스케줄러 락(자동 생성성)
- ` M Assets/AddressableAssetsData/AddressableAssetSettings.asset` — Addressable 설정 (확인 필요: 효과 아이콘 등록 흔적인지 점검)
- `?? Assets/RelicFairy/Characters/Monster/Boss_Monster/Lich/Lich.prefab.bak.meta` — 고아 .bak.meta
- `?? Assets/RelicFairy/UI/RootUI/@UIRoot.prefab.bak.meta` — 고아 .bak.meta
- `?? "Assets/_Recovery/0 (4).unity"` ~ `0 (6).unity` (+ .meta) — Unity 복구 씬 파일(불필요 시 제외)

> 주의: `.bak.meta` 고아 파일은 원본 `.bak` 없이 `.meta`만 존재 → Unity 콘솔 경고 가능. 정리 시 .meta 직접 삭제는 프로젝트 규칙상 지양이나, 짝 없는 .bak.meta는 예외적 정리 대상(확인 필요).

---

## 6. 백업 / 참고 위치

| 용도 | 경로 | 상태 |
|---|---|---|
| 레거시 1·2순위 정리 백업 | `C:\tmp\legacy_cleanup_20260627_120252\` | 존재 |
| 로그인 레거시 백업 | `C:\tmp\login_legacy_backup_20260617_130308\` | 존재 |
| 상점 정리 백업 | `C:\tmp\shop_backup_20260616\` | 존재 |
| @UIRoot 정리 백업 | `C:\tmp\uiroot_backup_20260616_155016\` | 존재 |
| 출시 체크리스트 | `docs/release-checklist.md` | 존재 (커밋됨) |

### 관련 설계 문서 (`docs/`)
`item-system-design.md`, `item-tier3-tier4-design.md`, `covenant-system-v2-design.md` / `-impl-spec.md`, `merlin-rune-elements-design.md`, `procgen-design.md`, `hades-save-design.md`, `relic-class-design.md`, `hero-relic-design.md`, `audio-mixer-design.md`, `basecamp-design.md`, `boss-custom-arena-design.md`, `camera-system-analysis.md`, `monster-attack-ux-analysis.md`, `map-room-system-guide.md`, `lee-pink-reimport-guide.md`, `release-checklist.md`, `reviews/`

---

## 7. 다음 작업자가 바로 할 일 (순서)

1. **미커밋 효과텍스트 A~D 검증 후 커밋**
   - Unity `refresh_unity` → `read_console` 로 컴파일 에러 0 확인 (프로젝트 규칙).
   - 에디터 플레이로 시각 검증: 아이템 획득 팝업 / 인벤 상세·툴팁 / HUD 버프 칩 / 아이템 정보 패널의 효과 텍스트·아이콘(플레이스홀더) 레이아웃.
   - **커밋 전략**: 효과텍스트 표시 레이어(신규 6 .cs + 수정 4 .cs)를 **한 커밋**으로. 폰트 SDF / `.claude` / `_Recovery` / `.bak.meta` 는 **포함하지 말 것**(별도 판단).

2. **E단계 착수** (§3 E)
   - ① 출처별 어댑터로 누락 지점(상점 카드/룬/유물 PassiveSO/서약/스킬 툴팁) 위젯 적용
   - ② `GuidelineVisualRunner` statusId 어휘를 `EffectMetaRegistry`/`ElementDef` 로 통합
   - ③ `QuestTracker.prefab`/`QuestNotifier.prefab` 폰트 NotoSansKR→DNF 전환

3. **폰트 SDF 결정** (§4-1): 옵션 A(checkout 복원) vs C(플래그 OFF). C는 사용자 의도 확인 후 진행.

4. **stale 참조 정정** (§4-3): `.claude/agents/client.md:45`, `uiux.md:31` 2줄.

5. **레거시 3순위**(§4-2): 기획 결정 받은 뒤에만 삭제 진행.

6. **push**: 현재 ahead 1(HEAD `32b9b899d`). 메모리 정책상 push는 사용자 명시 요청 시에만.

---

### 부록 — 실측 근거 명령
- 브랜치/HEAD/ahead·behind: `git rev-parse`, `git rev-list --left-right --count @{u}...HEAD`
- 상태: `git status --porcelain=v1`
- 커밋 통계: `git show --stat 32b9b899d`
- 조상 확인: `git merge-base --is-ancestor a7fc1ab67 HEAD` → YES
- 파일 존재: `ls Assets/RelicFairy/Shared/Effect/`, `ls -d /c/tmp/*`
- 폰트 플래그: `grep m_ClearDynamicDataOnBuild / m_AtlasPopulationMode`
- IconKey: `grep -oE '"[A-Za-z_]+"' EffectIconRegistry.cs`
